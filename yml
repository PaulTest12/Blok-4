name: CI Test

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

steps:
  - name: Chectout repository
    uses: actions/checkout@v3

  - name: Run echo command
    run: echo "Hello from GitHub Actions"

Add Ci workflow
